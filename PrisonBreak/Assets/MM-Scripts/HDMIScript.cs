using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HDMIScript : MonoBehaviour
{
    //will want a reference to the camera that it is in
    //should have a bool for whether this is the reciever 
    //if it is, then you can exclude it from certain checks
    //
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
